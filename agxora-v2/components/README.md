# `components/`

Canonical UI implementations live in **`app/components/`** (Next.js App Router colocation).

Use the path alias:

```ts
import { AppShell } from "@/components/AppShell";
```

This top-level folder reserves the standard AGXORA layout. Do not add a second parallel component tree here.
