# `providers/`

Canonical app providers live in **`app/providers/`** (e.g. `AppProviders`).

Use the path alias:

```ts
import { AppProviders } from "@/providers/AppProviders";
```

This top-level folder reserves the standard AGXORA layout.
