# `lib/`

Canonical libraries live in **`app/lib/`** (AI, auth, business, theme, SaaS, etc.).

Use the path alias:

```ts
import { useTheme } from "@/lib/theme";
```

This top-level folder reserves the standard AGXORA layout. Do not duplicate modules here.
