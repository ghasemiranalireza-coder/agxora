"use client";

/**
 * Documents dialog — thin alias of the shared Dialog primitive.
 * Kept for import stability; behavior lives in `app/components/ui/Dialog`.
 */

export { Dialog as DocumentsDialog } from "../../ui/Dialog";
