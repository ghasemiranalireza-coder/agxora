# `hooks/`

Shared React hooks that are not route-colocated.

Prefer `@/hooks/...` imports once modules are added here.
Existing domain hooks currently live under `app/lib/**` (e.g. `app/lib/memory/useMemory.ts`).
